#ifndef _INFORMATION_H_
#define _INFORMATION_H_

//#include "Packet.h"
#include <cstddef>

class Information {
public:
    //Information(Packet& packet, size_t peer_id);

private:
    //Packet packet_;
    size_t peer_id_;
};

#endif